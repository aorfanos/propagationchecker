name: test
on: [push]

jobs:
  run:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        domains: 
          - "aorfanos.com"
          - "in.gr"

    steps:
      - uses: actions/checkout@v1
      
      - run: |
          ./propagationChecker.sh -c A aorfanos.com
